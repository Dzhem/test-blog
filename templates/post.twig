{% extends 'base.twig' %}

{% block body %}
	<div class="container">
		<img src="https://dummyimage.com/800x400/8970eb/e6e6f2" class="img-fluid d-block mx-auto" alt="...">
		<h1 class="mt-3 mb-3">{{ post.title|escape }}</h1>
		<span class="text-muted">Имя автора |
			{{ post.published_date|date("F d, Y") }}</span>
		<p>{{ post.content|raw }}</p>
	</div>

{% endblock %}
